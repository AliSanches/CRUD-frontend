export default function Enviar({type}) {
    return (
        <button type={type} className="mx-3 bg-violet-500 hover:bg-violet-600 active:bg-violet-700 focus:outline-none focus:ring focus:border-sky-500 p-2 rounded-lg text-white">Cadastrar</button>
    );
}